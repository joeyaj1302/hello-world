#include <stdlib.h>
#include <stdio.h>

int main() {
    int x = 10;
    int y = 10;
    printf("THe operator precedence can be seen as in  7*x / 2*y = %d\n", 7*x / 2*y);
    printf("THe operator precedence can be seen as in   10.0*x / 20*y= %f", 10.0*x / 20*y);
    return 0;
}